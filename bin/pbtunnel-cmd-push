#!/bin/bash
commands['push']='push snapshot from client to server'

cmd_push_prepare() {
    return
}

cmd_push_exec() {
    empty_callback() {
        return
    }

    snapshot_series_response_diff snapshots

    snapshot_receive_all empty_callback "${snapshots[@]}"
}

